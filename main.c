#include <stdio.h>
#include "euler.h"
int main()
{
    printf("(1) The sum of all multiples of 3 & 5 less than 1000 is: %d\n", three_five(1000));
    printf("(5) The lcm of 1-20 is: %d\n", lcm_upto(20));
    return 0;
}